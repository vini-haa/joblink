import React from 'react';

const JobSearch = () => {
    return (
        <div>
            <h2>Pesquisa de Vagas</h2>
            <input type="text" placeholder="Pesquise por posição, localização, etc." />
            <button type="submit">Pesquisar</button>
        </div>
    );
};

export default JobSearch;
